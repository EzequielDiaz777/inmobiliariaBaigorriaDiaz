@model IEnumerable<inmobiliariaBaigorriaDiaz.Models.TipoDeInmueble>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoDeInmueble)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdTipoDeInmueble)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IdTipoDeInmueble }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdTipoDeInmueble }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdTipoDeInmueble})
                </td>
            </tr>
        }
    </tbody>
</table>
